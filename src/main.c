// Custom helper
# include <helpers.h>

int main(void) {
    if(initialize_uart_interface()) {
        
    }

    if(configure_uart_port()){

    }

    if(close(g_fd)){
        
    }

    return 0;
}

